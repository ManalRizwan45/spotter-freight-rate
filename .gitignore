### Spotter-supplied assessment materials.
# data/ is committed: december_chart_inputs.csv with its predicted_rate column filled is
# a deliverable, and committing the rest means the pipeline runs on a fresh clone with
# nothing to place by hand. The assessment PDF stays out because nothing here reads it;
# docs/assessment_readme.md carries the instructions that matter.
Freight_Rate_ML_Assessment.pdf

### Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/

.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/

### Run output
# Deliverables (validation_predictions.csv, december_predictions.csv,
# reports/figures/) are committed deliberately.
artifacts/
scorer_results/

.DS_Store
Thumbs.db
