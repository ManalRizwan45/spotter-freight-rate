### Spotter-supplied assessment materials.
# data/ is committed: the repository is private and shared only with the reviewers who
# supplied these files, and one of them - december_chart_inputs.csv with its
# predicted_rate column filled - is a deliverable. The assessment PDF stays out because
# nothing here reads it; docs/assessment_readme.md carries the instructions that matter.
Freight_Rate_ML_Assessment.pdf

### Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/

.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/

### Run output
# Deliverables (validation_predictions.csv, december_predictions.csv,
# reports/figures/) are committed deliberately.
artifacts/
scorer_results/

.DS_Store
Thumbs.db
